**Note:** This uses the same props as the `<CheckboxListInline />` component, but has a different tag name. Functionally, they are identical other than their layout.

```
<CheckboxList
  options={options}

  handleChange={handleChange}
/>
```