**Note:** This uses the same props as the `<RadioListInline />` component, but has a different tag name. Functionally, they are identical other than their layout.

```xml
<RadioList
  options={options}

  handleChange={handleChange}
/>
```